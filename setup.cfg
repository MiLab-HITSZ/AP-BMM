[metadata]
name = APBMM
version = 0.1.0
description = Core code release for the AP-BMM paper

[options]
packages = find:
package_dir =
    = src
python_requires = >=3.10

[options.packages.find]
where = src
